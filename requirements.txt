# ============================================================
# Crop Recommendation System — Python Dependencies
# Compatible with Python 3.12
# ============================================================

# --- Web Framework ---
fastapi==0.111.0
uvicorn[standard]==0.29.0
# python-multipart removed: no endpoint accepts a form or upload, and the
# package carries 7 known advisories.

# --- Data Validation ---
pydantic==2.7.1
pydantic-settings==2.2.1

# --- Machine Learning ---
scikit-learn==1.4.2
numpy==1.26.4
pandas==2.2.2
joblib==1.4.2

# --- Explainable AI ---
shap==0.45.0

# --- MLOps ---
mlflow==2.12.2
dvc==3.50.1

# mlflow 2.12.2 imports pkg_resources at module load. setuptools 81+ removed
# it, and Python 3.12 venvs no longer ship setuptools at all, so a clean
# `pip install -r requirements.txt` used to fail on `import mlflow`.
# Remove this pin once mlflow is upgraded (>=3.11.1 also clears 43 advisories).
setuptools<81

# --- Visualization (backend SHAP plots) ---
matplotlib==3.8.4

# --- Testing ---
pytest==8.2.0
pytest-asyncio==0.23.6
httpx==0.27.0          # also the transport used by fastapi.testclient

# Configuration is read from environment variables directly (see
# backend/main.py), so python-dotenv is not required.
